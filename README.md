# PDF 重排工具

> 扫描版PDF字块放大重排工具 - 让压缩成一条的扫描PDF变得可读

![Python](https://img.shields.io/badge/Python-3.8%2B-blue)
![License](https://img.shields.io/badge/License-MIT-green)

## ✨ 功能特点

- 📄 **字块放大重排** - 将扫描版PDF压缩的字块放大并重新排版
- 🔄 **竖版自动转横版** - 竖排古籍自动转为横版布局
- 🎯 **OCR边界框检测** - 使用 Tesseract OCR 检测文字位置
- 🖥️ **图形界面 + 命令行** - 支持 GUI 和 CLI 两种使用方式
- ⚙️ **可配置参数** - 放大倍数、字符间距、行间距等可调

## 📸 效果对比

| 处理前 | 处理后 |
|--------|--------|
| 字符压缩在顶部，不可读 | 字符分散排列，横版可读 |

## 🚀 快速开始

### 安装依赖

```bash
# Python 包
pip install -r requirements.txt

# 系统依赖
sudo apt install poppler-utils tesseract-ocr tesseract-ocr-chi-sim
```

### 使用方法

#### GUI 模式
```bash
python3 -m src.main --gui
```

#### 命令行模式
```bash
# 基本用法
python3 -m src.main input.pdf output.pdf

# 自定义参数
python3 -m src.main input.pdf output.pdf --scale 2.0 --dpi 300
```

## 📦 单文件可执行版

从 [Releases](https://github.com/yourusername/pdf-retypeset/releases) 下载 `pdf-retypeset` 可执行文件，直接运行：

```bash
chmod +x pdf-retypeset
./pdf-retypeset --gui
```

## 🛠️ 技术方案

- **图像处理**: OpenCV 形态学操作 + 连通域分析
- **OCR检测**: Tesseract pytesseract 边界框检测
- **PDF处理**: PyMuPDF, pdf2image
- **GUI**: Tkinter

## 📂 项目结构

```
pdf-retypeset/
├── src/
│   ├── main.py          # CLI入口
│   ├── gui.py           # Tkinter GUI
│   ├── config.py        # 配置管理
│   ├── pipeline.py      # 处理流水线
│   ├── preprocessor.py  # 图像预处理
│   ├── segmenter.py     # 字块分割
│   ├── layouter.py      # 重新布局
│   └── output.py        # PDF输出
├── config.yaml          # 配置文件
├── requirements.txt     # 依赖列表
└── README.md
```

## ⚙️ 配置参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--scale` | 字符放大倍数 | 1.5 |
| `--dpi` | 输入DPI | 300 |
| `--char-spacing` | 字符间距 | 10 |
| `--line-spacing` | 行间距 | 20 |
| `--page-size` | 输出页面大小 | A4 |

## 📝 开发记录

详细开发记录请查看：[PDF重排工具开发记录_整合版.md](PDF重排工具开发记录_整合版.md)

## ⚠️ 已知问题

- "一"字和标点符号显示稍大（完成度97%，不影响使用）
- OCR处理较慢（14页PDF约需1-2分钟）

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📧 联系方式

如有问题，请提交 GitHub Issue。
