# PDF 重排工具 - 使用指南

## 📦 打包单文件可执行程序

### 方法1：使用构建脚本（推荐）

```bash
cd /home/wu/桌面/pdf-retypeset
./build.sh
```

打包成功后，可执行文件位于：`dist/pdf-retypeset`

### 方法2：手动打包

```bash
# 1. 安装 PyInstaller
pip3 install pyinstaller --break-system-packages

# 2. 打包
pyinstaller build.spec

# 3. 可执行文件在 dist/ 目录
```

### 使用可执行文件

```bash
# 赋予执行权限
chmod +x dist/pdf-retypeset

# GUI 模式
./dist/pdf-retypeset --gui

# 命令行模式
./dist/pdf-retypeset input.pdf output.pdf --scale 1.5
```

### 部署到其他 Linux 电脑

1. 将 `dist/pdf-retypeset` 复制到目标电脑
2. 赋予执行权限：`chmod +x pdf-retypeset`
3. 直接运行：`./pdf-retypeset --gui`

**注意**：目标电脑仍需安装系统依赖：
```bash
sudo apt install poppler-utils tesseract-ocr tesseract-ocr-chi-sim
```

---

## 🚀 推送到 GitHub

### 方法1：使用推送脚本（推荐）

```bash
cd /home/wu/桌面/pdf-retypeset
./github-push.sh
```

按提示输入你的 GitHub 仓库地址。

### 方法2：手动推送

```bash
# 1. 初始化仓库
cd /home/wu/桌面/pdf-retypeset
git init

# 2. 添加文件
git add .

# 3. 提交
git commit -m "Initial commit: PDF 重排工具 v1.0"

# 4. 添加远程仓库（替换成你的地址）
git remote add origin https://github.com/yourusername/pdf-retypeset.git

# 5. 推送
git push -u origin main
```

### 首次推送前准备

1. 在 GitHub 创建新仓库：https://github.com/new
2. 仓库名：`pdf-retypeset`
3. 设置为 Public 或 Private
4. 不要初始化 README（我们已经有 README.md）
5. 创建后复制仓库地址

---

## 📋 文件说明

| 文件 | 说明 |
|------|------|
| `build.sh` | 打包脚本 - 生成单一可执行文件 |
| `github-push.sh` | GitHub 推送脚本 - 一键推送到仓库 |
| `build.spec` | PyInstaller 配置文件 |
| `README.md` | 项目说明文档 |
| `LICENSE` | MIT 许可证 |
| `.gitignore` | Git 忽略文件配置 |

---

## ⚠️ 注意事项

### 打包相关问题

1. **文件较大** - PyInstaller 打包的文件通常 50-100MB
2. **系统依赖** - 目标系统仍需安装 poppler 和 tesseract
3. **启动较慢** - 首次运行需要解压文件到临时目录

### GitHub 推送相关问题

1. **首次推送** - 需要配置 GitHub 访问令牌或 SSH 密钥
2. **仓库地址** - 确保替换成你自己的仓库地址
3. **主分支名称** - GitHub 默认使用 `main`，旧仓库可能是 `master`

---

## 🎯 下一步

1. ✅ 运行 `./build.sh` 打包可执行文件
2. ✅ 测试 `./dist/pdf-retypeset --gui` 确认可用
3. ✅ 运行 `./github-push.sh` 推送到 GitHub
4. ✅ 在 GitHub Releases 发布可执行文件

祝使用愉快！ 🎉
